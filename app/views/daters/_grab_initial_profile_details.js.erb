document.addEventListener("DOMContentLoaded", function(event) { 
  var app = angular.module('kinected');

  app.factory('grabInitialProfileDetails', function(){
    return <%= format_user_for_angular(@dater).to_json.html_safe %>;
  });
});