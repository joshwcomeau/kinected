document.addEventListener("DOMContentLoaded", function(event) { 
  var app = angular.module('kinected');

  app.factory('ProfileList', function(){
    return <%= @daters.to_json.html_safe %>;
  });
});
